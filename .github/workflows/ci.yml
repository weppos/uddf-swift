---
name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test-macos:
    name: Test (macOS)
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Swift version
        run: swift --version

      - name: Build
        run: swift build

      - name: Run tests
        run: swift test

  test-linux:
    name: Test (Linux)
    runs-on: ubuntu-latest
    container:
      image: swift:5.9

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Swift version
        run: swift --version

      - name: Build
        run: swift build

      - name: Run tests
        run: swift test

  # iOS tests disabled - macOS and Linux provide sufficient coverage
  # Uncomment when specific iOS simulator testing is needed
  # test-ios:
  #   name: Test (iOS)
  #   runs-on: macos-latest
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Build and test for iOS
  #       run: |
  #         xcodebuild test \
  #           -scheme UDDF \
  #           -destination 'generic/platform=iOS Simulator' \
  #           -enableCodeCoverage YES

  # coverage:
  #   name: Coverage
  #   runs-on: macos-latest
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Generate coverage
  #       run: swift test --enable-code-coverage
  #
  #     - name: Convert coverage to lcov
  #       run: |
  #         xcrun llvm-cov export -format="lcov" \
  #           .build/debug/UDDFPackageTests.xctest/Contents/MacOS/UDDFPackageTests \
  #           -instr-profile .build/debug/codecov/default.profdata > coverage.lcov
  #
  #     - name: Upload coverage to Codecov
  #       uses: codecov/codecov-action@v4
  #       with:
  #         files: coverage.lcov
  #         fail_ci_if_error: false
  #       env:
  #         CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
